---
phase: 09-native-text-rendering
plan: 03
type: execute
wave: 3
depends_on: [09-02]
files_modified:
  - MDViewerTests/NativeRendererTests.swift
autonomous: false
requirements:
  - NATV-02
  - NATV-03
  - PERF-02

must_haves:
  truths:
    - "NativeRenderer correctly renders headings, paragraphs, bold, italic, code, links, lists, blockquotes"
    - "NativeRenderer uses Latin Modern Roman font (not system font)"
    - "Native rendering path displays content visually matching the WKWebView path"
    - "Warm launch to first visible content is under 100ms for NSTextView path"
  artifacts:
    - path: "MDViewerTests/NativeRendererTests.swift"
      provides: "Unit tests for NativeRenderer AST-to-NSAttributedString conversion"
      min_lines: 80
  key_links:
    - from: "MDViewerTests/NativeRendererTests.swift"
      to: "MDViewer/NativeRenderer.swift"
      via: "@testable import MDViewer; NativeRenderer().render(root:)"
      pattern: "NativeRenderer"
---

<objective>
Add unit tests for NativeRenderer covering all supported markdown node types, then visually verify the native rendering path matches the WKWebView output and meets the sub-100ms PERF-02 target.

Purpose: Validates correctness of the AST-to-NSAttributedString conversion (NATV-02), typography matching (NATV-03), and performance target (PERF-02). The visual checkpoint catches rendering differences that unit tests cannot detect.

Output: NativeRendererTests.swift with comprehensive tests, plus human verification of visual output and performance.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-native-text-rendering/09-01-SUMMARY.md
@.planning/phases/09-native-text-rendering/09-02-SUMMARY.md

@MDViewer/NativeRenderer.swift
@MDViewer/MarkdownRenderer.swift
@MDViewerTests/MarkdownRendererTests.swift

<interfaces>
From MDViewer/NativeRenderer.swift (Plan 01):
```swift
struct NativeRenderResult {
    let attributedString: NSAttributedString
}

final class NativeRenderer {
    static func registerFonts()
    func render(root: OpaquePointer) -> NativeRenderResult
}
```

From MDViewer/MarkdownRenderer.swift (Plan 02):
```swift
final class MarkdownRenderer {
    func parseMarkdown(_ markdown: String) -> OpaquePointer?
    func canRenderNatively(root: OpaquePointer) -> Bool
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create NativeRendererTests</name>
  <read_first>
    - MDViewerTests/MarkdownRendererTests.swift
    - MDViewer/NativeRenderer.swift
    - MDViewer/MarkdownRenderer.swift
  </read_first>
  <files>MDViewerTests/NativeRendererTests.swift</files>
  <action>
Create `MDViewerTests/NativeRendererTests.swift` following the test patterns from MarkdownRendererTests.swift.

**Helper method** -- create a private helper that parses markdown and renders natively:
```swift
private func renderNative(_ markdown: String) -> NSAttributedString {
    let markdownRenderer = MarkdownRenderer()
    guard let root = markdownRenderer.parseMarkdown(markdown) else {
        XCTFail("Failed to parse markdown")
        return NSAttributedString()
    }
    defer { cmark_node_free(root) }

    let nativeRenderer = NativeRenderer()
    let result = nativeRenderer.render(root: root)
    return result.attributedString
}
```

**Test cases to implement:**

1. `testHeadingRendersWithLargerFont` -- render "# Heading 1", assert the attributed string text contains "Heading 1", assert the font size at range [0] is 32pt (h1 = 2em * 16).

2. `testParagraphRendersText` -- render "Hello world", assert string contains "Hello world".

3. `testBoldRendersWithBoldTrait` -- render "**bold text**", find range of "bold text", assert font at that range has bold trait (NSFontDescriptor.SymbolicTraits.bold).

4. `testItalicRendersWithItalicTrait` -- render "*italic text*", find range of "italic text", assert font has italic trait.

5. `testInlineCodeUsesMonoFont` -- render "Use `code` here", find range of "code", assert font is monospace (check font name contains "Mono" or "mono").

6. `testCodeBlockRendersContent` -- render triple-backtick code block, assert the code content appears in the attributed string.

7. `testLinkHasURLAttribute` -- render "[example](https://example.com)", find range of "example", assert .link attribute is URL "https://example.com".

8. `testUnorderedListHasBullets` -- render "- item 1\n- item 2", assert string contains bullet characters or "item 1" and "item 2".

9. `testOrderedListHasNumbers` -- render "1. first\n2. second", assert string contains numbering.

10. `testBlockquoteRendersContent` -- render "> quote text", assert "quote text" appears in string.

11. `testHorizontalRuleRenders` -- render "---", assert string is non-empty.

12. `testStrikethroughHasAttribute` -- render "~~struck~~", find range of "struck", assert .strikethroughStyle attribute is present.

13. `testCanRenderNativelyRejectsTable` -- parse markdown with GFM table, call canRenderNatively, assert false.

14. `testCanRenderNativelyRejectsMermaid` -- parse markdown with mermaid code block, call canRenderNatively, assert false.

15. `testCanRenderNativelyAcceptsSimpleMarkdown` -- parse simple markdown (headings, paragraphs, code), call canRenderNatively, assert true.

16. `testNestedFormattingBoldInItalic` -- render "*text **bold** more*", assert both traits appear at correct ranges.

17. `testMultipleHeadingLevels` -- render h1, h2, h3 headings, assert decreasing font sizes.

18. `testEmptyMarkdownReturnsEmptyString` -- render "", assert attributedString.length == 0 or minimal.

**Font tests** (NATV-03):
19. `testBodyTextUsesLatinModernFont` -- render "Hello", check font name at range [0] contains "Latin Modern" or the registered font name. If font registration has not happened, call NativeRenderer.registerFonts() first in setUp.

**Registration:**
Override `setUp()` to call `NativeRenderer.registerFonts()` once (or use a static flag).

Follow naming convention: `test<Feature><Scenario>`.
Import cmark_gfm at top for cmark_node_free.
  </action>
  <verify>
    <automated>cd /Users/mit/Documents/GitHub/mdviewer && xcodebuild test -project MDViewer.xcodeproj -scheme MDViewer -destination 'platform=macOS' -only-testing:MDViewerTests/NativeRendererTests 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - MDViewerTests/NativeRendererTests.swift exists and contains `final class NativeRendererTests: XCTestCase`
    - MDViewerTests/NativeRendererTests.swift contains `import cmark_gfm`
    - MDViewerTests/NativeRendererTests.swift contains `@testable import MDViewer`
    - MDViewerTests/NativeRendererTests.swift contains at least 15 test methods (test functions starting with `func test`)
    - MDViewerTests/NativeRendererTests.swift contains tests for: heading, bold, italic, code, link, list, blockquote, strikethrough, canRenderNatively
    - All tests pass when run via xcodebuild test
  </acceptance_criteria>
  <done>NativeRendererTests.swift has 18+ test cases covering all supported node types, canRenderNatively pre-scan, and Latin Modern font verification. All tests pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification and performance measurement</name>
  <what-built>
    Native rendering path: simple markdown files (no mermaid, no tables) now render via NSTextView instead of WKWebView. Typography should match the WKWebView path visually (Latin Modern Roman font, same heading sizes, code styling, link colors).
  </what-built>
  <how-to-verify>
    1. Build and run the app: `open MDViewer.xcodeproj` then Cmd+R
    2. Open a simple markdown file (e.g., a README.md with headings, paragraphs, bold, italic, code blocks, links, lists)
    3. Verify it renders in NSTextView (no web process in Activity Monitor)
    4. Compare visually with the web rendering: use View > Toggle Native/Web Rendering (Cmd+Shift+N) to switch to WKWebView and back
    5. Check typography: Latin Modern Roman font (not San Francisco system font), heading sizes proportional, code blocks styled with background
    6. Open a markdown file WITH a mermaid block or GFM table -- verify it opens in WKWebView (mermaid diagrams render, table renders)
    7. Test Cmd+P (Print) from both native and web windows
    8. Performance: Open Instruments, profile with OSSignposter, open a simple markdown file -- the "open-to-paint" interval for the NSTextView path should be under 100ms
    9. Test View > Toggle Monospace in both modes
  </how-to-verify>
  <resume-signal>Type "approved" if visual output matches and performance is under 100ms, or describe any issues (font mismatch, layout problems, performance concerns)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test input -> cmark parser | Test markdown strings fed to parser |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-08 | Information Disclosure | Test files | accept | Tests use synthetic markdown, no sensitive data |
</threat_model>

<verification>
1. All NativeRendererTests pass: `xcodebuild test -scheme MDViewer -only-testing:MDViewerTests/NativeRendererTests`
2. All existing MarkdownRendererTests still pass (no regressions)
3. Visual output matches WKWebView rendering for simple markdown
4. Sub-100ms warm launch confirmed via Instruments OSSignposter
</verification>

<success_criteria>
- 18+ test cases covering all markdown node types pass
- canRenderNatively correctly rejects tables and mermaid, accepts simple content
- Visual rendering matches WKWebView typography (human verified)
- NSTextView path warm launch under 100ms (PERF-02, human verified via Instruments)
</success_criteria>

<output>
After completion, create `.planning/phases/09-native-text-rendering/09-03-SUMMARY.md`
</output>
