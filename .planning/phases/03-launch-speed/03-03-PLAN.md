---
phase: 03-launch-speed
plan: 03
type: execute
wave: 3
depends_on: ["03-01", "03-02"]
files_modified:
  - .planning/phases/03-launch-speed/03-PROFILING.md
autonomous: false
requirements: [LAUNCH-02, LAUNCH-03]
gap_closure: true

must_haves:
  truths:
    - "Warm launch-to-paint interval is measured via Instruments os_signpost on Apple Silicon hardware"
    - "Cold launch-to-paint interval is measured via Instruments os_signpost on Apple Silicon hardware"
    - "03-PROFILING.md contains actual measured values, not estimates"
    - "LAUNCH-03 sub-100ms warm launch target is confirmed or refuted with measured data"
  artifacts:
    - path: ".planning/phases/03-launch-speed/03-PROFILING.md"
      provides: "Actual measured cold and warm launch timing data replacing estimates"
      contains: "Measured"
  key_links: []
---

<objective>
Close the two verification gaps from 03-VERIFICATION.md by having the user run Instruments profiling on the Release build and recording actual os_signpost measurements. No code changes needed -- the signpost instrumentation (Plan 01) and WKWebView pre-warm (Plan 02) are already implemented and verified correct.

Purpose: The ROADMAP success criterion requires "measured via os_signpost" -- estimated values do not satisfy this. One Instruments session closes both gaps.
Output: Updated 03-PROFILING.md with real measured timing data replacing all estimate rows.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-launch-speed/03-VERIFICATION.md
@.planning/phases/03-launch-speed/03-PROFILING.md
@.planning/phases/03-launch-speed/03-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Run Instruments profiling and record actual launch timing</name>
  <files>.planning/phases/03-launch-speed/03-PROFILING.md</files>
  <read_first>
    - .planning/phases/03-launch-speed/03-PROFILING.md (current estimates to be replaced)
    - .planning/phases/03-launch-speed/03-VERIFICATION.md (gaps requiring measurement)
    - MDViewer/main.swift (verify launchSignposter beginInterval is present)
    - MDViewer/AppDelegate.swift (verify launchSignposter endInterval and preWarmedContentView are present)
  </read_first>
  <action>
    Build the Release configuration for profiling:
    ```
    xcodebuild -project MDViewer.xcodeproj -scheme MDViewer -configuration Release build
    ```

    Then present the user with the exact profiling steps below.
  </action>
  <what-built>Plans 01 and 02 added launch-to-paint signpost instrumentation and WKWebView pre-warm. The Release build contains os_signpost intervals that appear in Instruments. This task requires the user to run Instruments and report the actual timing numbers.</what-built>
  <how-to-verify>
    **Profiling steps (warm launch -- this is the primary measurement):**

    1. Build Release: `xcodebuild -project MDViewer.xcodeproj -scheme MDViewer -configuration Release build`
    2. Locate the built app: `find ~/Library/Developer/Xcode/DerivedData -name "MDViewer.app" -path "*/Release/*" | head -1`
    3. Open Instruments.app (Cmd+Space, type "Instruments")
    4. Choose the "os_signpost" template (or "Logging" template which includes os_signpost)
    5. Set the target to the Release MDViewer.app binary
    6. Prepare a test markdown file (any .md file, ~1-10KB is representative)
    7. **First run (pre-warm the system):** Launch MDViewer with the test file, wait for render, quit
    8. **Second run (warm launch measurement):** In Instruments, click Record, launch MDViewer with the test file as argument, wait for content to appear, stop recording
    9. In the Instruments timeline, find the "RenderingPipeline" category under os_signpost
    10. Record the duration of these intervals:
        - `launch-to-paint` (the full end-to-end measurement)
        - `open-to-paint` (from openFile to first paint)
        - `file-read`, `parse`, `chunk-split` (per-stage breakdowns if visible)

    **Cold launch (secondary measurement):**

    11. Run `sudo purge` to clear disk cache (or reboot)
    12. Repeat steps 8-10 for a cold launch recording

    **Alternative quick method (if Instruments is cumbersome):**
    You can also add a temporary `os_log` print in `webContentViewDidFinishFirstPaint` to print the elapsed time to Console.app, but the os_signpost intervals in Instruments are the gold standard per ROADMAP criteria.

    **Report format -- please provide these numbers:**
    ```
    launch-to-paint (warm): ___ms
    launch-to-paint (cold): ___ms
    open-to-paint (warm): ___ms
    WKWebView init estimate from trace: ___ms (approximate, from gap in trace)
    Device: ___ (e.g., M4 Max, 48GB)
    macOS version: ___
    Test file: ___ (name and approximate size)
    ```
  </how-to-verify>
  <verify>
    <automated>cd /Users/mit/Documents/GitHub/mdviewer && xcodebuild -project MDViewer.xcodeproj -scheme MDViewer -configuration Release build 2>&1 | tail -5</automated>
  </verify>
  <resume-signal>Provide the profiling numbers in the format above. If warm launch-to-paint is under 100ms, LAUNCH-03 is confirmed. If over 100ms, we need to investigate further optimization.</resume-signal>
  <acceptance_criteria>
    - Release build compiles successfully
    - User provides actual measured launch-to-paint values (not estimates)
    - Both cold and warm launch numbers are reported
  </acceptance_criteria>
  <done>User has provided actual Instruments profiling numbers for cold and warm launch-to-paint intervals.</done>
</task>

<task type="auto">
  <name>Task 2: Update 03-PROFILING.md with measured values</name>
  <files>.planning/phases/03-launch-speed/03-PROFILING.md</files>
  <read_first>
    - .planning/phases/03-launch-speed/03-PROFILING.md (current file with estimates to replace)
  </read_first>
  <action>
    Using the actual profiling numbers provided by the user in Task 1, rewrite 03-PROFILING.md:

    1. Replace the "estimated values" disclaimer at the top with "Measured values from Instruments os_signpost profiling"
    2. Update the Device line with the user's actual hardware (e.g., "Apple M4 Max, macOS 26.x")
    3. Update the Test file line with the actual test file used
    4. Replace ALL "(est.)" columns in the Timing Data table with actual measured values
    5. Update the "Decision: WKWebView Pre-Warm" section:
       - Change "WKWebView init cost (estimated)" to "WKWebView init cost (measured)" with the actual value
       - Change "Evidence" from industry estimates to "Instruments os_signpost trace on [device]"
       - Keep "Decision: Implemented" (the code is already in place)
    6. Update the "Pre-Warm Impact" table with actual before/after if available (the "after" is what was measured since pre-warm is already active; "before" can remain estimated or be removed)
    7. Remove the "Validation Steps" section at the bottom (validation is now complete)
    8. Add a "Measurement Confirmation" section at the bottom stating:
       - Whether warm launch-to-paint is under 100ms (LAUNCH-03 status)
       - Date of measurement
       - Instruments template used

    If warm launch-to-paint is OVER 100ms, add a "Further Optimization Needed" subsection noting the gap and suggesting next steps (e.g., template pre-load into pre-warmed WKWebView per research Pattern 3).

    If warm launch-to-paint is UNDER 100ms, add "LAUNCH-03 CONFIRMED" as a clear marker.
  </action>
  <verify>
    <automated>cd /Users/mit/Documents/GitHub/mdviewer && grep -c "est\." .planning/phases/03-launch-speed/03-PROFILING.md</automated>
  </verify>
  <acceptance_criteria>
    - 03-PROFILING.md contains no "(est.)" labels -- all values are measured (grep for "est." returns 0)
    - 03-PROFILING.md contains "Measured" in the header note (grep-checkable)
    - 03-PROFILING.md contains either "LAUNCH-03 CONFIRMED" or "Further Optimization Needed" (grep-checkable)
    - The disclaimer "These values are estimates" is removed
    - Device, macOS version, and test file are filled in with actual values
  </acceptance_criteria>
  <done>03-PROFILING.md contains actual measured timing data from Instruments. No estimate disclaimers remain. LAUNCH-03 status is explicitly recorded as confirmed or unmet.</done>
</task>

</tasks>

<verification>
1. `grep -c "est\." .planning/phases/03-launch-speed/03-PROFILING.md` returns 0 (no estimates remain)
2. `grep "Measured" .planning/phases/03-launch-speed/03-PROFILING.md` shows measured values header
3. `grep -E "LAUNCH-03 CONFIRMED|Further Optimization Needed" .planning/phases/03-launch-speed/03-PROFILING.md` shows explicit status
4. `grep "launch-to-paint" .planning/phases/03-launch-speed/03-PROFILING.md` shows actual ms values (not ranges with ~)
</verification>

<success_criteria>
- 03-PROFILING.md contains actual Instruments-measured cold and warm launch-to-paint times
- All estimate disclaimers and "(est.)" labels are removed
- LAUNCH-03 sub-100ms warm launch target is explicitly confirmed or refuted with measured data
- WKWebView pre-warm decision evidence is updated from industry estimates to actual measurement
</success_criteria>

<output>
After completion, create `.planning/phases/03-launch-speed/03-03-SUMMARY.md`
</output>
