---
phase: 06-vendored-cmark
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Vendor/cmark-gfm/src/
  - Vendor/cmark-gfm/extensions/
  - project.yml
  - Package.swift
autonomous: true
requirements:
  - CMARK-01

must_haves:
  truths:
    - "App builds with vendored cmark sources compiled directly in the Xcode project"
    - "SPM swift-cmark dependency is fully removed"
    - "import cmark_gfm and import cmark_gfm_extensions resolve in Swift code"
  artifacts:
    - path: "Vendor/cmark-gfm/src/html.c"
      provides: "Vendored HTML renderer source"
    - path: "Vendor/cmark-gfm/src/include/module.modulemap"
      provides: "Swift module mapping for cmark_gfm"
    - path: "Vendor/cmark-gfm/extensions/include/module.modulemap"
      provides: "Swift module mapping for cmark_gfm_extensions"
    - path: "project.yml"
      provides: "Build configuration with static lib targets"
      contains: "cmark-gfm"
  key_links:
    - from: "project.yml"
      to: "Vendor/cmark-gfm/src/"
      via: "sources and HEADER_SEARCH_PATHS"
      pattern: "Vendor/cmark-gfm/src"
    - from: "MDViewer target"
      to: "cmark-gfm target"
      via: "target dependency"
      pattern: "target: cmark-gfm"
---

<objective>
Vendor swift-cmark C sources into the Xcode project and configure the build system so the app compiles and links against project-owned static libraries instead of the SPM remote dependency.

Purpose: CMARK-01 requires eliminating the SPM dependency. This is the foundation for all subsequent Phase 6 work (chunked API, mermaid detection, extension caching).
Output: Vendor/cmark-gfm/ directory with C sources, updated project.yml with static lib targets, SPM files removed, app builds and existing tests pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-vendored-cmark/06-RESEARCH.md
@.planning/phases/06-vendored-cmark/06-PATTERNS.md

<interfaces>
<!-- Build system: current project.yml structure -->
From project.yml:
```yaml
packages:
  swift-cmark:
    url: https://github.com/apple/swift-cmark.git
    branch: gfm
targets:
  MDViewer:
    dependencies:
      - package: swift-cmark
        product: cmark-gfm
      - package: swift-cmark
        product: cmark-gfm-extensions
```

<!-- Source location for vendoring -->
swift-cmark source at: /Users/mit/Documents/GitHub/swift-cmark
- src/ directory: ~25 C files + include/ with headers and module.modulemap
- extensions/ directory: ~7 C files + include/ with headers and module.modulemap

<!-- Module maps that must be preserved -->
src/include/module.modulemap defines: module cmark_gfm
extensions/include/module.modulemap defines: module cmark_gfm_extensions
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Vendor cmark-gfm sources and remove SPM dependency</name>
  <files>
    Vendor/cmark-gfm/src/ (new directory, copied from /Users/mit/Documents/GitHub/swift-cmark/src/)
    Vendor/cmark-gfm/extensions/ (new directory, copied from /Users/mit/Documents/GitHub/swift-cmark/extensions/)
    Package.swift (delete)
  </files>
  <read_first>
    /Users/mit/Documents/GitHub/swift-cmark/src/include/module.modulemap
    /Users/mit/Documents/GitHub/swift-cmark/extensions/include/module.modulemap
    /Users/mit/Documents/GitHub/mdviewer/Package.swift
  </read_first>
  <action>
1. Create directory `Vendor/cmark-gfm/` at the project root.
2. Copy the entire `src/` directory from `/Users/mit/Documents/GitHub/swift-cmark/src/` to `Vendor/cmark-gfm/src/`. This includes all `.c` files, `.inc` files, and the `include/` subdirectory with headers and `module.modulemap`.
3. Copy the entire `extensions/` directory from `/Users/mit/Documents/GitHub/swift-cmark/extensions/` to `Vendor/cmark-gfm/extensions/`. This includes all `.c`, `.h` files, and the `include/` subdirectory with its `module.modulemap`.
4. Delete `Package.swift` from the project root (SPM manifest no longer needed).
5. Delete `MDViewer.xcodeproj/project.xcworkspace/xcshareddata/swiftpm/Package.resolved` if it exists (SPM lockfile no longer needed).
6. Do NOT copy any `.re` files from the source -- actually, they can be copied but will be excluded by XcodeGen. The copy is a full directory copy; exclusion happens in project.yml.

Verification: `Vendor/cmark-gfm/src/html.c` exists, `Vendor/cmark-gfm/src/include/module.modulemap` exists, `Vendor/cmark-gfm/extensions/include/module.modulemap` exists, `Package.swift` does not exist.
  </action>
  <verify>
    <automated>test -f Vendor/cmark-gfm/src/html.c && test -f Vendor/cmark-gfm/src/include/module.modulemap && test -f Vendor/cmark-gfm/extensions/include/module.modulemap && test ! -f Package.swift && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - File exists: Vendor/cmark-gfm/src/html.c
    - File exists: Vendor/cmark-gfm/src/include/cmark-gfm.h
    - File exists: Vendor/cmark-gfm/src/include/module.modulemap containing "module cmark_gfm"
    - File exists: Vendor/cmark-gfm/extensions/include/module.modulemap containing "module cmark_gfm_extensions"
    - File exists: Vendor/cmark-gfm/extensions/core-extensions.c
    - File does NOT exist: Package.swift
    - Count of .c files in Vendor/cmark-gfm/src/ is >= 20
    - Count of .c files in Vendor/cmark-gfm/extensions/ is >= 5
  </acceptance_criteria>
  <done>swift-cmark sources are vendored at Vendor/cmark-gfm/ and SPM Package.swift is removed</done>
</task>

<task type="auto">
  <name>Task 2: Configure project.yml with static library targets and regenerate Xcode project</name>
  <files>
    project.yml
    MDViewer.xcodeproj/project.pbxproj (regenerated by xcodegen)
  </files>
  <read_first>
    /Users/mit/Documents/GitHub/mdviewer/project.yml
    /Users/mit/Documents/GitHub/swift-cmark/Package.swift
  </read_first>
  <action>
Rewrite `project.yml` to replace the SPM dependency with two static library targets and update the MDViewer target:

```yaml
name: MDViewer
settings:
  base:
    MACOSX_DEPLOYMENT_TARGET: "13.0"
    SWIFT_VERSION: "5.9"
    GENERATE_INFOPLIST_FILE: NO
targets:
  cmark-gfm:
    type: library.static
    platform: macOS
    sources:
      - Vendor/cmark-gfm/src
    settings:
      HEADER_SEARCH_PATHS:
        - $(SRCROOT)/Vendor/cmark-gfm/src/include
        - $(SRCROOT)/Vendor/cmark-gfm/extensions/include
      MODULEMAP_FILE: $(SRCROOT)/Vendor/cmark-gfm/src/include/module.modulemap
      GCC_C_LANGUAGE_STANDARD: c99
    excludes:
      - "*.re"
      - "*.in"
      - CMakeLists.txt
  cmark-gfm-extensions:
    type: library.static
    platform: macOS
    sources:
      - Vendor/cmark-gfm/extensions
    settings:
      HEADER_SEARCH_PATHS:
        - $(SRCROOT)/Vendor/cmark-gfm/src/include
        - $(SRCROOT)/Vendor/cmark-gfm/extensions/include
      MODULEMAP_FILE: $(SRCROOT)/Vendor/cmark-gfm/extensions/include/module.modulemap
      GCC_C_LANGUAGE_STANDARD: c99
    dependencies:
      - target: cmark-gfm
    excludes:
      - "*.re"
      - CMakeLists.txt
  MDViewer:
    type: application
    platform: macOS
    sources:
      - MDViewer
    resources:
      - MDViewer/Resources
    settings:
      INFOPLIST_FILE: MDViewer/Info.plist
      PRODUCT_BUNDLE_IDENTIFIER: com.mdviewer.app
      HEADER_SEARCH_PATHS:
        - $(SRCROOT)/Vendor/cmark-gfm/src/include
        - $(SRCROOT)/Vendor/cmark-gfm/extensions/include
    dependencies:
      - target: cmark-gfm
      - target: cmark-gfm-extensions
  MDViewerTests:
    type: bundle.unit-test
    platform: macOS
    sources:
      - MDViewerTests
    settings:
      GENERATE_INFOPLIST_FILE: YES
    dependencies:
      - target: MDViewer
```

Key changes from current project.yml:
- REMOVED: entire `packages:` block (lines 7-10 of current file)
- ADDED: `cmark-gfm` static library target with sources from Vendor/cmark-gfm/src
- ADDED: `cmark-gfm-extensions` static library target with sources from Vendor/cmark-gfm/extensions
- CHANGED: MDViewer dependencies from `package: swift-cmark` to `target: cmark-gfm` and `target: cmark-gfm-extensions`
- ADDED: HEADER_SEARCH_PATHS to MDViewer target for cmark header resolution

After writing project.yml, run `xcodegen generate` to regenerate the Xcode project. If xcodegen is not available, the user will need to run it manually.

Then build the project: `xcodebuild build -project MDViewer.xcodeproj -scheme MDViewer -configuration Debug -quiet`

If `.inc` files cause build issues (XcodeGen tries to compile them), add `"*.inc"` to the cmark-gfm excludes list and regenerate.
  </action>
  <verify>
    <automated>cd /Users/mit/Documents/GitHub/mdviewer && xcodegen generate 2>&1 && xcodebuild build -project MDViewer.xcodeproj -scheme MDViewer -configuration Debug -quiet 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - project.yml contains "type: library.static" (appears twice, once for each C target)
    - project.yml does NOT contain "packages:" or "swift-cmark"
    - project.yml MDViewer target has "target: cmark-gfm" in dependencies
    - project.yml MDViewer target has HEADER_SEARCH_PATHS containing "Vendor/cmark-gfm/src/include"
    - `xcodegen generate` succeeds without errors
    - `xcodebuild build -scheme MDViewer` succeeds (BUILD SUCCEEDED)
    - `xcodebuild test -scheme MDViewer` (existing tests pass -- MarkdownRendererTests)
  </acceptance_criteria>
  <done>App builds against vendored cmark static libraries with no SPM dependency. All existing tests pass. CMARK-01 satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| vendored C source | Copied from a trusted local checkout of apple/swift-cmark; no network fetch |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | Vendor/cmark-gfm/ | accept | Source copied from known local git checkout at verified revision 924936d; no network involved |
</threat_model>

<verification>
1. `xcodebuild build -scheme MDViewer` succeeds
2. `xcodebuild test -scheme MDViewer` -- all existing MarkdownRendererTests pass
3. `grep -r "swift-cmark" project.yml` returns no results
4. `test -d Vendor/cmark-gfm/src/include` confirms vendored structure
</verification>

<success_criteria>
- App compiles and links against vendored cmark-gfm and cmark-gfm-extensions static libraries
- No SPM references remain in project configuration
- `import cmark_gfm` and `import cmark_gfm_extensions` resolve in MarkdownRenderer.swift
- All existing unit tests pass without modification
</success_criteria>

<output>
After completion, create `.planning/phases/06-vendored-cmark/06-01-SUMMARY.md`
</output>
