---
phase: 11-ci-notarized-release
plan: 02
type: execute
wave: 2
depends_on: [11-01]
files_modified:
  - .github/workflows/release.yml
  - docs/release/ci-secrets.md
  - .planning/phases/11-ci-notarized-release/11-DRY-RUN-LOG.md
autonomous: false
requirements:
  - CI-09
  - CI-12
  - CI-13

must_haves:
  truths:
    - "When the workflow completes successfully, a GitHub Release exists for the tag, marked as a DRAFT (not yet published), with the notarized stapled DMG attached as a release asset"
    - "docs/release/ci-secrets.md exists and documents every required secret (MAC_CERT_P12_BASE64, MAC_CERT_P12_PASSWORD, KEYCHAIN_PASSWORD, APPLE_TEAM_ID, ASC_API_KEY_ID, ASC_API_ISSUER_ID, ASC_API_KEY_P8_BASE64, SPARKLE_ED_PRIVATE_KEY, HOMEBREW_TAP_PAT) with: how to generate it, how to install it as a GitHub secret, the role/scope minimum, the rotation procedure, and the consequences of loss"
    - "An end-to-end CI release dry-run has been executed against the v2.2.0-rc.1 pre-release tag and produced a notarized stapled DMG attached to a draft pre-release on the GitHub Release page"
    - "The dry-run output is captured in 11-DRY-RUN-LOG.md so future humans can audit what was actually verified before the real v2.2.0 cut"
  artifacts:
    - path: ".github/workflows/release.yml"
      provides: "Tag-triggered workflow now also creates a draft GitHub Release with the DMG attached (extends the file from plan 11-01)"
      contains: "softprops/action-gh-release"
    - path: "docs/release/ci-secrets.md"
      provides: "Operator runbook for every CI secret: source, install, rotation, loss-impact"
      contains: "ASC_API_KEY_P8_BASE64"
    - path: ".planning/phases/11-ci-notarized-release/11-DRY-RUN-LOG.md"
      provides: "Captured evidence from the v2.2.0-rc.1 dry-run: workflow run URL, durations, asset SHAs, stapler validation output"
      contains: "v2.2.0-rc.1"
  key_links:
    - from: ".github/workflows/release.yml"
      to: "softprops/action-gh-release@v2"
      via: "gh-release step uploads build/MDViewer-${VERSION}.dmg with draft: true"
      pattern: "draft:\\s*true"
    - from: "docs/release/ci-secrets.md"
      to: ".github/workflows/release.yml"
      via: "every secret named in the workflow has a corresponding section in the doc"
      pattern: "MAC_CERT_P12_BASE64"
    - from: ".planning/phases/11-ci-notarized-release/11-DRY-RUN-LOG.md"
      to: "the v2.2.0-rc.1 GitHub Release"
      via: "log records the published release URL and the stapler validate output"
      pattern: "v2.2.0-rc.1"
---

<objective>
Take the build/sign/notarize pipeline from plan 11-01 and turn it into a real, drafted release: extend the workflow with the GitHub Release publish step (draft mode), write the operator-grade secrets documentation, then execute the full pipeline end-to-end against a `v2.2.0-rc.1` pre-release tag and capture the evidence. After this plan completes, all 13 CI requirements are satisfied and the pipeline has been proven on a real tag — the developer is cleared to cut `v2.2.0` proper.

Purpose: Plan 11-01 produces a stapled DMG sitting on the runner. Without a publish step, that DMG is a 30-day workflow artifact that disappears — useless for distribution. This plan adds the GitHub Release upload (CI-09 — DRAFT mode so the developer manually flips to published after a Sparkle smoke test in Phase 12), writes the operator runbook (CI-12 — without it, key rotation becomes a forensic exercise), and runs the actual dry-run against `v2.2.0-rc.1` (CI-13 — the explicit pre-release gate from the success criteria, identified in REQUIREMENTS.md "Critical Pre-Release Gates" #3).

Output: An updated `.github/workflows/release.yml` with the draft-release step appended; a new `docs/release/ci-secrets.md` (~9 secret sections, each ~30 lines); and a `11-DRY-RUN-LOG.md` capturing the v2.2.0-rc.1 dry-run evidence. The Apple-Actions/import-codesign-certs SHA placeholder left in plan 11-01 is also resolved here — the dry-run cannot run with a placeholder.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/research/v2.2/ARCHITECTURE.md
@.planning/research/v2.2/PITFALLS.md
@.planning/research/v2.2/STACK.md
@.planning/research/v2.2/FEATURES.md

@.planning/phases/11-ci-notarized-release/11-01-PLAN.md
@.planning/phases/11-ci-notarized-release/11-01-SUMMARY.md

@.github/workflows/release.yml
@MDViewer/Info.plist
@dist/RELEASE_NOTES.md

<interfaces>
<!--
  Reference contracts the executor needs. Do NOT explore the codebase — these
  are reproduced inline.
-->

## Workflow file from plan 11-01 (already on disk after that plan ran)

`.github/workflows/release.yml` ends with the dSYM upload step and a comment block marking the planned 11-02 insertion point:

```yaml
      # Plan 11-02 will add the next steps here:
      #   - softprops/action-gh-release@v2 with draft: true (CI-09)
      #     - upload build/MDViewer-${VERSION}.dmg as release asset
```

Append to that file; do NOT rewrite it from scratch.

## Outputs from plan 11-01 available at the GitHub Release publish step

- `build/MDViewer-${VERSION}.dmg` — stapled, validated DMG (the release asset)
- `build/notarytool-submit.json` — already uploaded as `notarytool-log` artifact
- `build/notarytool-log.json` — already uploaded as `notarytool-log` artifact (only on failure)
- `build/MDViewer.xcarchive/dSYMs/` — already uploaded as `MDViewer-${VERSION}-dSYMs` artifact

Step ID `version` from plan 11-01 exposes:
- `${{ steps.version.outputs.tag }}`     # e.g. `v2.2.0-rc.1`
- `${{ steps.version.outputs.version }}` # e.g. `2.2.0-rc.1`

## Required GitHub Actions secrets (must exist BEFORE the dry-run)

These are documented in `docs/release/ci-secrets.md` (created in this plan), but the
workflow consumes them by name:

| Secret | Used by | Plan introducing |
|---|---|---|
| `MAC_CERT_P12_BASE64` | release.yml (Apple-Actions/import-codesign-certs) | 11-01 |
| `MAC_CERT_P12_PASSWORD` | release.yml (Apple-Actions/import-codesign-certs) | 11-01 |
| `KEYCHAIN_PASSWORD` | release.yml (used by Apple-Actions internally; some recipes pass explicitly) | 11-01 |
| `APPLE_TEAM_ID` | release.yml (env, scripts) | 11-01 |
| `ASC_API_KEY_ID` | release.yml (notarize step env) | 11-01 |
| `ASC_API_ISSUER_ID` | release.yml (notarize step env) | 11-01 |
| `ASC_API_KEY_P8_BASE64` | release.yml (notarize step env) | 11-01 |
| `SPARKLE_ED_PRIVATE_KEY` | release.yml (Phase 12 — NOT used by this phase) | 12-XX |
| `HOMEBREW_TAP_PAT` | release.yml (Phase 13 — NOT used by this phase) | 13-XX |

The doc covers all nine because phase 11 is "release pipeline" — operators need
the full picture before phases 12/13 land. Sections for SPARKLE/HOMEBREW are
clearly marked "introduced in Phase 12/13 — not yet consumed by release.yml".

## softprops/action-gh-release@v2 contract

```yaml
- uses: softprops/action-gh-release@v2
  with:
    tag_name: ${{ steps.version.outputs.tag }}
    name: MDViewer ${{ steps.version.outputs.version }}
    draft: true                 # CI-09
    prerelease: false           # set to true for v*-rc.* tags (handled in workflow logic)
    generate_release_notes: true   # GitHub-native PR-title-driven notes (FEATURES.md §2)
    files: |
      build/MDViewer-${VERSION}.dmg
    fail_on_unmatched_files: true
```

Pin to a commit SHA, same hardening rationale as `Apple-Actions/import-codesign-certs`.
At execution time:

```bash
gh api repos/softprops/action-gh-release/git/refs/tags/v2 --jq .object.sha
```

Use `.object.sha` (the SHA the v2 mutable tag currently points to). Substitute into
the YAML.

## Pre-release vs release detection

Plan 11-02's success criterion #6 says "draft by default". For `vX.Y.Z-rc.N` tags
we additionally want `prerelease: true`. Implement with a workflow expression:

```yaml
prerelease: ${{ contains(steps.version.outputs.tag, '-') }}
```

`v2.2.0` → no `-` → prerelease=false; `v2.2.0-rc.1` → has `-` → prerelease=true.
Both stay `draft: true`.

## DO NOT in this plan

- DO NOT remove `draft: true` ever (CI-09; Phase 12 Sparkle integration depends on the developer manually publishing only after a smoke test — auto-publish would point the appcast at unverified builds)
- DO NOT add the Sparkle appcast generation step (that's Phase 12 / SPK-06)
- DO NOT add the Homebrew cask bump step (that's Phase 13 / BREW-06)
- DO NOT skip the `v2.2.0-rc.1` dry-run (this is the explicit Critical Pre-Release Gate #3 from REQUIREMENTS.md)

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append the draft-release publish step to release.yml and resolve the action SHA placeholders</name>
  <read_first>
    - .github/workflows/release.yml (current state from plan 11-01)
    - .planning/research/v2.2/FEATURES.md (§2 — softprops/action-gh-release, draft-then-publish, generate_release_notes)
    - .planning/research/v2.2/ARCHITECTURE.md (§3 — workflow shape; the publish step belongs at the end of the same job)
  </read_first>
  <files>.github/workflows/release.yml</files>
  <action>
**Step 1: Resolve the `Apple-Actions/import-codesign-certs@v6.0.0` SHA left as a placeholder by plan 11-01.**

```bash
IC_SHA=$(gh api repos/Apple-Actions/import-codesign-certs/git/refs/tags/v6.0.0 --jq .object.sha)
echo "import-codesign-certs v6.0.0 SHA: $IC_SHA"
sed -i '' "s/REPLACE_WITH_V6_0_0_SHA/$IC_SHA/" .github/workflows/release.yml
```

If `gh` is not authenticated in the executor environment, halt and surface the issue — the dry-run in Task 3 cannot run with an unpinned action.

**Step 2: Resolve the `softprops/action-gh-release@v2` SHA.**

```bash
GHR_SHA=$(gh api repos/softprops/action-gh-release/git/refs/tags/v2 --jq .object.sha)
echo "action-gh-release v2 SHA: $GHR_SHA"
```

Capture the SHA — substituted into the YAML below.

**Step 3: Append the publish step to `.github/workflows/release.yml`.**

Locate the comment block at the end of the file:

```
      # Plan 11-02 will add the next steps here:
      #   - softprops/action-gh-release@v2 with draft: true (CI-09)
      ...
```

REPLACE that comment block with the actual steps. Final tail of the file:

```yaml
      # CI-09: GitHub Release as a DRAFT. Developer manually flips draft → published
      # after smoke-testing the DMG locally. This is intentional — Sparkle (Phase 12)
      # depends on the appcast pointing at a verified release; auto-publish here
      # would expose users to unverified builds.
      - name: Create draft GitHub Release (CI-09)
        uses: softprops/action-gh-release@<GHR_SHA>
        with:
          tag_name: ${{ steps.version.outputs.tag }}
          name: MDViewer ${{ steps.version.outputs.version }}
          draft: true
          # vX.Y.Z-rc.N → prerelease=true; vX.Y.Z → prerelease=false
          prerelease: ${{ contains(steps.version.outputs.tag, '-') }}
          generate_release_notes: true
          files: |
            build/MDViewer-${{ steps.version.outputs.version }}.dmg
          fail_on_unmatched_files: true
```

Substitute `<GHR_SHA>` with the actual SHA from Step 2 (do NOT leave the literal placeholder).

**Step 4: YAML sanity check.**

```bash
python3 -c "import yaml,sys; yaml.safe_load(open('.github/workflows/release.yml'))" && echo "YAML OK"
```

Also confirm both action references are now SHA-pinned (40 hex chars):

```bash
grep -E "uses: (Apple-Actions/import-codesign-certs|softprops/action-gh-release)@" .github/workflows/release.yml
# Expected: both lines end with @<40 hex chars>, no @v6.0.0 / @v2 / placeholder.
```
  </action>
  <verify>
    <automated>cd /Users/mit/Documents/GitHub/mdviewer && python3 -c "import yaml; yaml.safe_load(open('.github/workflows/release.yml'))" && grep -q "draft: true" .github/workflows/release.yml && grep -q "prerelease: \${{ contains(steps.version.outputs.tag, '-') }}" .github/workflows/release.yml && grep -q "generate_release_notes: true" .github/workflows/release.yml && grep -q "fail_on_unmatched_files: true" .github/workflows/release.yml && ! grep -q "REPLACE_WITH" .github/workflows/release.yml && grep -E "uses: Apple-Actions/import-codesign-certs@[0-9a-f]{40}" .github/workflows/release.yml && grep -E "uses: softprops/action-gh-release@[0-9a-f]{40}" .github/workflows/release.yml && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/release.yml` parses as valid YAML
    - Workflow contains a `softprops/action-gh-release@<40-hex SHA>` step (NOT `@v2`, NOT a placeholder)
    - That step has `draft: true`, `prerelease: ${{ contains(steps.version.outputs.tag, '-') }}`, `generate_release_notes: true`, `fail_on_unmatched_files: true`
    - That step uploads `build/MDViewer-${{ steps.version.outputs.version }}.dmg`
    - The `Apple-Actions/import-codesign-certs` reference is now SHA-pinned (40 hex chars), not `@v6.0.0` and not `REPLACE_WITH_V6_0_0_SHA`
    - The string `REPLACE_WITH` does not appear anywhere in the workflow file
  </acceptance_criteria>
  <done>release.yml extended with the draft-release publish step (CI-09); both third-party actions are pinned to commit SHAs; YAML validates. The workflow is now feature-complete for Phase 11 — Phases 12 and 13 will append their own steps in their own plans.</done>
</task>

<task type="auto">
  <name>Task 2: Write docs/release/ci-secrets.md operator runbook</name>
  <read_first>
    - .planning/research/v2.2/STACK.md (§3 ASC API key generation, §4 cert import)
    - .planning/research/v2.2/PITFALLS.md (#2 EdDSA loss = permanent lockout; #13 .p8 newline mangling)
    - .planning/research/v2.2/ARCHITECTURE.md (§4 — Secrets Architecture table; the doc must mirror this)
    - REQUIREMENTS.md (CI-12 secret list — exact 9 names)
  </read_first>
  <files>docs/release/ci-secrets.md</files>
  <action>
Create `docs/release/ci-secrets.md`. Use `mkdir -p docs/release` first. The doc has one major section per secret. Structure:

```markdown
# CI Release Secrets — Operator Runbook

This document is the canonical source of truth for every secret consumed by
`.github/workflows/release.yml` (Phase 11) and the planned Sparkle / Homebrew
extensions in Phases 12 and 13. If you are setting up the CI release pipeline
from scratch, work through every section in order.

**Audience:** the human operating the `mitgor/mdviewer` repo. Likely only one
person (the developer). The detail level assumes you may be doing this once
every 18 months — written for the rotation case, not the repeat case.

**Status legend in section headers:**
- ACTIVE — consumed by the current `release.yml` (Phase 11)
- PHASE 12 — will be consumed once Sparkle integration ships
- PHASE 13 — will be consumed once Homebrew cask ships

---

## Quick start

1. Open <https://github.com/mitgor/mdviewer/settings/secrets/actions>.
2. Work through every ACTIVE section below. For each: generate the secret per
   "How to generate", install via "How to install" with the exact name in the
   section header.
3. Before cutting a real `v*` tag, run a `v*-rc.*` dry-run (Plan 11-02 Task 3).
   If the dry-run produces a stapled DMG attached to a draft pre-release, all
   ACTIVE secrets are correctly installed.
4. PHASE 12 and PHASE 13 secrets can wait until those phases land — they will
   not break the current workflow.

---

## Inventory

| Secret name | Status | Purpose |
|---|---|---|
| `MAC_CERT_P12_BASE64` | ACTIVE | Developer ID Application cert + private key (base64-encoded .p12) |
| `MAC_CERT_P12_PASSWORD` | ACTIVE | Password used during the .p12 export |
| `KEYCHAIN_PASSWORD` | ACTIVE | Throwaway password for the per-run CI keychain |
| `APPLE_TEAM_ID` | ACTIVE | `V7YK72YLFF` (Woodenshark LLC) |
| `ASC_API_KEY_ID` | ACTIVE | 10-char Key ID from App Store Connect Team API Key |
| `ASC_API_ISSUER_ID` | ACTIVE | UUID Issuer ID for the Team API Key |
| `ASC_API_KEY_P8_BASE64` | ACTIVE | base64 of the AuthKey_*.p8 file (one-time download) |
| `SPARKLE_ED_PRIVATE_KEY` | PHASE 12 | EdDSA private key for signing appcast updates |
| `HOMEBREW_TAP_PAT` | PHASE 13 | Fine-grained PAT for the homebrew-tap repo |

---
```

Then one section per secret. Use this template for each:

```markdown
## `MAC_CERT_P12_BASE64`  (ACTIVE)

**Purpose:** base64-encoded `.p12` containing the "Developer ID Application:
Woodenshark LLC (V7YK72YLFF)" certificate AND its private key. Imported by
`Apple-Actions/import-codesign-certs` into a per-run ephemeral keychain.

### How to generate

1. On the developer Mac (the one with the keychain holding the cert), open
   Keychain Access.
2. Search for "Developer ID Application". You will likely see TWO certs with
   the same CN — that is expected (PROJECT.md notes both are valid; the
   workflow disambiguates by SHA-1 at runtime via `Scripts/import_cert.sh`).
   Pick the NEWER one (longer expiration date).
3. Right-click → Export → set format to "Personal Information Exchange
   (.p12)" → save as `mdviewer-developer-id.p12`.
4. Set a strong password when prompted. **Save this password** — it goes into
   `MAC_CERT_P12_PASSWORD` next.
5. base64-encode the .p12:
   ```bash
   base64 -i mdviewer-developer-id.p12 | pbcopy
   ```
   Your clipboard now holds the value to paste into the GitHub Secret.

### How to install

1. Go to <https://github.com/mitgor/mdviewer/settings/secrets/actions>.
2. Click "New repository secret".
3. Name: `MAC_CERT_P12_BASE64` (exact, case-sensitive).
4. Value: paste from clipboard.
5. Click "Add secret".

### Role / scope minimum

- The cert must be of type "Developer ID Application" (not "Developer ID
  Installer", not "Apple Distribution"). Notary will reject other types.
- It must NOT be expired. `security find-identity -v` only lists non-expired
  certs; `Scripts/import_cert.sh` relies on this.

### Rotation procedure

Triggers for rotation:
- Cert is approaching expiry (~6 months before — Apple Developer ID certs are
  typically valid for 5 years).
- The `.p12` password leaks (re-export with a new password, replace both
  `MAC_CERT_P12_BASE64` and `MAC_CERT_P12_PASSWORD`).
- The cert is revoked from the Apple Developer portal.

Rotation steps:
1. If the existing cert is not expired/revoked, generate a NEW Developer ID
   Application cert in the Apple Developer portal (Keychain Access → Certificate
   Assistant → Request a Certificate from a CA → upload the CSR to
   developer.apple.com → download → install).
2. Repeat the export above with the new cert.
3. Update both `MAC_CERT_P12_BASE64` and `MAC_CERT_P12_PASSWORD` in the same
   sitting (atomic).
4. Run a dry-run with a fresh `v*-rc.*` tag to confirm the new cert signs
   correctly. Both certs will live in the runner keychain during the overlap
   period; `Scripts/import_cert.sh` picks the first valid match — verify it
   picks the new one by checking `signing_id` output in the workflow log.

### Consequences of loss

LOW. The `.p12` is a re-exportable backup of a cert that lives in the developer
Mac's keychain. If the secret is lost (deleted by accident, repo deletion), the
developer can re-export from the keychain and reinstall. **However, if the
keychain copy is ALSO lost** (e.g. dev Mac wiped + no Time Machine backup):
- Generate a new cert in the Apple Developer portal (free, takes ~5 minutes).
- Note: a new cert means a new SHA-1, so any pinned references break — but the
  workflow does not pin, it discovers per run. Self-healing.
- Existing notarized DMGs remain notarized; only future builds need re-signing.

---
```

Repeat that template (with secret-specific content) for ALL nine secrets:

1. `MAC_CERT_P12_BASE64` (ACTIVE) — as above.
2. `MAC_CERT_P12_PASSWORD` (ACTIVE) — purpose: password set during step 4 of the .p12 export. How to install: same flow. Rotation: rotate together with `MAC_CERT_P12_BASE64`. Loss consequence: LOW (re-export with a new password).
3. `KEYCHAIN_PASSWORD` (ACTIVE) — purpose: random per-run password for the throwaway keychain that `Apple-Actions/import-codesign-certs` creates. How to generate: `openssl rand -base64 32 | pbcopy`. Rotation: rotate annually as hygiene. Loss consequence: NONE (the keychain it protects is destroyed at job teardown; just regenerate).
4. `APPLE_TEAM_ID` (ACTIVE) — purpose: the 10-char team ID used to filter ambiguous certs. How to generate: it's `V7YK72YLFF` (find on developer.apple.com → Membership → Team ID). NOT a real secret, but stored as one for symmetry. Rotation: only changes if the team is renamed or you change Apple Developer Programs. Loss: NONE (public information).
5. `ASC_API_KEY_ID` (ACTIVE) — 10-char Key ID. How to generate: App Store Connect → Users and Access → Integrations → Team Keys → click + → name "MDViewer CI" → role **Developer** (least privilege per STACK.md §3) → download the `.p8` (one-time, immediate, irrecoverable). The Key ID is shown in the keys list. How to install: paste the 10 chars. Rotation: every 12 months as hygiene. Loss: REPEATABLE — can revoke and recreate any time, but the .p8 is one-time-download so you also rotate `ASC_API_KEY_P8_BASE64`.
6. `ASC_API_ISSUER_ID` (ACTIVE) — UUID issuer ID, ONE per team, shared across all keys. How to generate: same App Store Connect page, shown above the keys list. Rotation: never changes. Loss: NONE (public to the team owner).
7. `ASC_API_KEY_P8_BASE64` (ACTIVE) — purpose: the actual private key. How to generate: `base64 -i AuthKey_<KEY_ID>.p8 | pbcopy` (Pitfall #13 — base64 to survive newline normalization in GitHub Secrets). How to install: paste. Rotation: only when the corresponding `ASC_API_KEY_ID` rotates (atomic with it). Loss: HIGH severity if no backup, because the .p8 is one-time download from Apple — but RECOVERABLE: revoke the dead key in App Store Connect, generate a new one, update both `ASC_API_KEY_ID` + `ASC_API_KEY_P8_BASE64` together. NEVER notarize-blocking longer than a manual ~10-minute rotation.

8. `SPARKLE_ED_PRIVATE_KEY` (PHASE 12) — purpose: EdDSA private key for signing the Sparkle appcast (Phase 12 / SPK-06). How to generate: after Phase 12's `generate_keys` SPM tool resolves, run `<sparkle-bin>/generate_keys` ONCE on the developer Mac, then `<sparkle-bin>/generate_keys -x sparkle_private_key.txt`, then `cat sparkle_private_key.txt | pbcopy`. How to install: paste. **Role:** the corresponding PUBLIC key goes into `Info.plist` as `SUPublicEDKey` and is committed to git (public by design). Rotation procedure: SEVERE — see "Consequences of loss" below. **Consequences of loss: CATASTROPHIC AND PERMANENT.** If lost, you cannot sign updates that existing v2.2+ installs will accept (they verify against the embedded `SUPublicEDKey`). Per Pitfall #2, "lose access to your EdDSA signing keys" produces a fallback to unsigned updates only after a long enough period — Sparkle does not document the exact threshold. Mitigation: per REQUIREMENTS.md "Critical Pre-Release Gate" #1, the EdDSA private key MUST be backed up to at least one offline location (1Password, hardware token, encrypted external drive) BEFORE the first Sparkle-enabled release ships. Document the backup location HERE in this section when the backup is created.

9. `HOMEBREW_TAP_PAT` (PHASE 13) — purpose: fine-grained PAT used by `macAuley/action-homebrew-bump-cask` to fork `mitgor/homebrew-tap` and open a bump PR (Phase 13 / BREW-06, BREW-07). How to generate: <https://github.com/settings/personal-access-tokens/new> → fine-grained → resource owner: `mitgor` → repository access: only `mitgor/homebrew-tap` → permissions: Contents: Read & Write, Pull requests: Read & Write → expiration: 12 months. Copy the token. How to install: paste. Why a PAT not GITHUB_TOKEN: per ARCHITECTURE.md §8, `GITHUB_TOKEN` is scoped to the running repo only and cannot fork. Rotation: every 12 months when the PAT expires. Loss: LOW (revoke the old PAT and create a new one; no in-flight state lost).

---

After all 9 sections, add a closing **"Critical pre-release gates checklist"** section that mirrors REQUIREMENTS.md "Critical Pre-Release Gates" with checkboxes:

```markdown
## Pre-release checklist

Before cutting the FIRST `v2.2.0` release (or any first-of-milestone release):

- [ ] All ACTIVE secrets are installed and a `v2.2.0-rc.1` dry-run produced a
      stapled DMG on a draft pre-release (Plan 11-02 Task 3, recorded in
      `.planning/phases/11-ci-notarized-release/11-DRY-RUN-LOG.md`).
- [ ] (Phase 12 only) `SPARKLE_ED_PRIVATE_KEY` is backed up offline to TWO
      documented locations and the backup procedure is recorded in this file.
- [ ] (Phase 12 only) `CFBundleVersion` migrated from string to monotonic
      integer (SPK-08). String comparison sort makes "10" < "2" — see
      Pitfall #19.
- [ ] (Phase 12 only) README has a one-time manual-update notice for v2.1
      users (Pitfall #9 — pre-Sparkle installs cannot auto-update).
- [ ] (Phase 13 only) Homebrew cask declares `auto_updates true` (Pitfall #10
      — without it, brew upgrade fights Sparkle).
```

The doc is intentionally verbose. Operator-grade docs trade brevity for "this
will still work in 2 years when you forget every detail".
  </action>
  <verify>
    <automated>cd /Users/mit/Documents/GitHub/mdviewer && test -f docs/release/ci-secrets.md && grep -q "MAC_CERT_P12_BASE64" docs/release/ci-secrets.md && grep -q "MAC_CERT_P12_PASSWORD" docs/release/ci-secrets.md && grep -q "KEYCHAIN_PASSWORD" docs/release/ci-secrets.md && grep -q "APPLE_TEAM_ID" docs/release/ci-secrets.md && grep -q "ASC_API_KEY_ID" docs/release/ci-secrets.md && grep -q "ASC_API_ISSUER_ID" docs/release/ci-secrets.md && grep -q "ASC_API_KEY_P8_BASE64" docs/release/ci-secrets.md && grep -q "SPARKLE_ED_PRIVATE_KEY" docs/release/ci-secrets.md && grep -q "HOMEBREW_TAP_PAT" docs/release/ci-secrets.md && grep -q "Rotation" docs/release/ci-secrets.md && grep -q "Consequences of loss" docs/release/ci-secrets.md && grep -q "V7YK72YLFF" docs/release/ci-secrets.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `docs/release/ci-secrets.md` exists
    - File mentions ALL nine secret names from REQUIREMENTS.md CI-12 (exact strings): `MAC_CERT_P12_BASE64`, `MAC_CERT_P12_PASSWORD`, `KEYCHAIN_PASSWORD`, `APPLE_TEAM_ID`, `ASC_API_KEY_ID`, `ASC_API_ISSUER_ID`, `ASC_API_KEY_P8_BASE64`, `SPARKLE_ED_PRIVATE_KEY`, `HOMEBREW_TAP_PAT`
    - Each secret has a "How to generate", "How to install", "Rotation procedure", and "Consequences of loss" section (greppable for "Rotation" and "Consequences of loss")
    - The Sparkle key section includes the offline-backup requirement (Critical Pre-Release Gate #1 from REQUIREMENTS.md)
    - The ASC API key section calls out the **Team Key** + **Developer role** minimum (STACK.md §3)
    - The ASC `.p8` section explicitly says "base64-encode" before paste (Pitfall #13)
    - File contains the team ID `V7YK72YLFF`
    - File contains a "Pre-release checklist" / "Critical pre-release gates" section
  </acceptance_criteria>
  <done>docs/release/ci-secrets.md exists with one full section per secret (9 sections), covering generation, installation, role minimum, rotation, and loss consequences. The doc mirrors ARCHITECTURE.md §4 secrets table but is operator-grade (verbose, written for the once-every-18-months rotation case). The Sparkle-key offline-backup gate from REQUIREMENTS.md is captured. CI-12 satisfied.</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 3: Execute end-to-end CI dry-run against v2.2.0-rc.1 and capture evidence</name>
  <gate>blocking</gate>
  <what-built>
    - Plans 11-01 and 11-02 (this plan) Tasks 1–2 have produced a complete signed/notarized/draft-published GitHub Actions workflow on a feature branch.
    - All nine secrets from `docs/release/ci-secrets.md` are installed in repo settings (developer must verify the seven ACTIVE ones; PHASE 12/13 ones can wait).
  </what-built>
  <how-to-verify>
The dry-run is THE explicit pre-release gate from REQUIREMENTS.md success criterion #6 / Critical Pre-Release Gate #3. It must run on a real GitHub-hosted `macos-26` runner against a real tag, against the real Apple notary service, with the real signing cert. Local mocks do not satisfy CI-13.

**Step A — Pre-flight (developer Mac).**

1. Confirm the branch with the workflow file is pushed to `origin`:
   ```bash
   git status                           # working tree clean
   git log --oneline -5                 # confirm latest commits include the workflow + scripts
   git push origin <branch>
   ```

2. Confirm all seven ACTIVE secrets are installed:
   ```bash
   gh secret list --repo mitgor/mdviewer
   ```
   Expected names present: `MAC_CERT_P12_BASE64`, `MAC_CERT_P12_PASSWORD`, `KEYCHAIN_PASSWORD`, `APPLE_TEAM_ID`, `ASC_API_KEY_ID`, `ASC_API_ISSUER_ID`, `ASC_API_KEY_P8_BASE64`. The Phase 12/13 secrets are optional for this dry-run.

3. Confirm the `Apple-Actions/import-codesign-certs` and `softprops/action-gh-release` lines in `.github/workflows/release.yml` are SHA-pinned (40 hex chars), NOT tags:
   ```bash
   grep -E "uses: (Apple-Actions/import-codesign-certs|softprops/action-gh-release)@" .github/workflows/release.yml
   ```

**Step B — Cut the dry-run tag.**

The workflow is on a non-default branch right now. Tags do not require a default-branch commit; a tag pointing at a branch HEAD will still trigger `on: push: tags: [v*]`. If you prefer to merge to main first, do so — but dry-run-from-branch is supported.

```bash
# Tag the current commit on the workflow branch:
git tag v2.2.0-rc.1
git push origin v2.2.0-rc.1

# Watch the workflow:
gh run watch
```

Expected: the workflow appears in <https://github.com/mitgor/mdviewer/actions> within ~10 seconds, runs on `macos-26`, and completes in <30 minutes.

**Step C — Inspect the workflow run while it runs.**

Verify these step outputs in real time (or after completion via `gh run view`):

1. "Discover signing identity SHA-1" step prints a 40-char hex string and writes `signing_id=<sha>` to outputs.
2. "Build + sign universal hardened app" step's `lipo -archs` output includes both `arm64` and `x86_64`.
3. "Notarize + staple" step exits 0 and prints `Notarization status: Accepted`.
4. "Verify release artifacts" step prints all `✓` checks.
5. "Create draft GitHub Release" step exits 0.

**Step D — Verify the GitHub Release was created as a draft pre-release.**

```bash
gh release view v2.2.0-rc.1 --repo mitgor/mdviewer
# Expected:
#   draft: true
#   prerelease: true       (because tag contains "-")
#   assets: MDViewer-2.2.0-rc.1.dmg
```

Also visit <https://github.com/mitgor/mdviewer/releases> in a browser. The release should appear as a "Draft" with a "Pre-release" tag.

**Step E — Download the DMG asset and verify it locally.**

```bash
mkdir -p /tmp/dry-run
cd /tmp/dry-run
gh release download v2.2.0-rc.1 --repo mitgor/mdviewer --pattern "*.dmg"

# Universal binary check
hdiutil attach -nobrowse MDViewer-2.2.0-rc.1.dmg
APP="/Volumes/MDViewer 2.2.0-rc.1/MDViewer.app"
lipo -info "$APP/Contents/MacOS/MDViewer"
# Expected: "Architectures in the fat file: ... are: x86_64 arm64" (or: arm64 x86_64)

# Hardened runtime check
codesign -dvvv "$APP" 2>&1 | grep -E "flags=.*runtime"
# Expected: line containing "runtime"

# Team check
codesign -dvvv "$APP" 2>&1 | grep TeamIdentifier
# Expected: TeamIdentifier=V7YK72YLFF

# Stapler check
xcrun stapler validate MDViewer-2.2.0-rc.1.dmg
# Expected: "The validate action worked!"

# Gatekeeper check
spctl -a -t open --context context:primary-signature -vv MDViewer-2.2.0-rc.1.dmg
# Expected: "accepted" with "source=Notarized Developer ID"

hdiutil detach "/Volumes/MDViewer 2.2.0-rc.1"
```

**Step F — Capture evidence in `.planning/phases/11-ci-notarized-release/11-DRY-RUN-LOG.md`.**

Create the file with this content (filling in actual values from the run):

```markdown
# Phase 11 CI Release Pipeline — v2.2.0-rc.1 Dry-Run Log

**Date:** YYYY-MM-DD
**Tag:** v2.2.0-rc.1
**Branch the tag was cut from:** <branch-name>
**Workflow run URL:** https://github.com/mitgor/mdviewer/actions/runs/<RUN_ID>
**Wall-clock duration:** ~XX minutes (target: <30 min — success criterion #1)

## Workflow steps observed

| Step | Status | Notes |
|---|---|---|
| Checkout | ✓ | |
| Pin Xcode 26.2 | ✓ | (or note actual default if 26.2 was already in PATH) |
| Stamp version into Info.plist | ✓ | CFBundleShortVersionString=2.2.0-rc.1, CFBundleVersion=2.2.0-rc.1 |
| Cache SPM dependencies | ✓ / cache-miss | (first run = miss, expected) |
| Import Developer ID cert | ✓ | |
| Discover signing identity SHA-1 | ✓ | signing_id=<actual SHA from run log> |
| Build + sign universal hardened app | ✓ | lipo output: arm64 x86_64 |
| Build + sign DMG | ✓ | DMG: MDViewer-2.2.0-rc.1.dmg |
| Notarize + staple | ✓ | submission_id=<actual UUID>, status=Accepted, ~X minutes wait |
| Verify release artifacts | ✓ | all 5 hard gates passed |
| Upload notarytool log | ✓ | (uploaded; status=Accepted so log file is empty/not present, the submit JSON IS uploaded) |
| Upload dSYM bundle | ✓ | artifact: MDViewer-2.2.0-rc.1-dSYMs |
| Create draft GitHub Release | ✓ | draft=true, prerelease=true |

## Local DMG verification

- `lipo -info`: `MDViewer.app` is `arm64 x86_64` ✓
- `codesign -dvvv` flags include `runtime` ✓
- `codesign -dvvv` shows `TeamIdentifier=V7YK72YLFF` ✓
- `xcrun stapler validate`: "The validate action worked!" ✓
- `spctl -a -t open`: "accepted" + "source=Notarized Developer ID" ✓
- DMG size: X.X MB
- DMG SHA-256: `<sha>`

## Issues encountered

(Document anything: notarization wait > 10 min, cache miss surprises, action SHA
resolution friction, etc. Empty list = clean run.)

## Sign-off

Pipeline approved for the real `v2.2.0` cut. All 13 CI requirements satisfied.

— signed: <developer name>, <date>
```

**Step G — Cleanup.**

After capturing evidence, the dry-run release should NOT remain as a public
draft (it could be accidentally published later). Either:

- Delete the draft release: `gh release delete v2.2.0-rc.1 --repo mitgor/mdviewer --yes`
- Delete the tag: `git push --delete origin v2.2.0-rc.1 && git tag -d v2.2.0-rc.1`

OR keep it as a real prerelease for further smoke testing in Phase 12 (Sparkle
update flow). Either is fine — document the choice in the dry-run log.

If ANY of steps A–F fail, fix the root cause (not the symptom), re-tag (`git tag
-d v2.2.0-rc.1; git push --delete origin v2.2.0-rc.1; git tag v2.2.0-rc.1; git
push origin v2.2.0-rc.1`), and re-run. Capture each iteration in the log.
  </how-to-verify>
  <resume-signal>
    Type "approved — dry-run logged" once `11-DRY-RUN-LOG.md` exists and reflects a successful Accepted notarization with a stapled DMG attached to a draft pre-release.

    OR type "failed — <root cause>" if the dry-run failed in a way that requires planning revision (e.g. a secret was not generatable in the documented form, an action behavior changed, runner image regressed).

    OR type "partial — <what passed, what didn't>" to capture an intermediate state.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions runner ↔ GitHub Releases API | `softprops/action-gh-release` step creates/uploads to a release using the workflow's `GITHUB_TOKEN` |
| Draft release ↔ end users | Draft releases are NOT visible to anonymous users; published releases ARE — the manual draft→published gate is a security boundary |
| Operator ↔ docs/release/ci-secrets.md | Plain-text doc in the repo; future operators rely on it for secret generation/rotation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-13 | Spoofing | `softprops/action-gh-release` upstream | mitigate | Pin to commit SHA of v2 (Task 1 Step 2), not the floating `@v2` tag |
| T-11-14 | Tampering | Auto-published release with unverified DMG | mitigate | `draft: true` is non-negotiable in CI-09 — developer manually flips draft → published only after a Sparkle smoke test (Phase 12) on the actual DMG asset |
| T-11-15 | Information Disclosure | `docs/release/ci-secrets.md` reveals secret structure but not values | accept | Doc is intentionally public (in repo). Names, generation procedures, and storage locations are not secret; the secret VALUES are in GitHub Secrets which are encrypted at rest and never logged |
| T-11-16 | Information Disclosure | Sparkle EdDSA key location written into ci-secrets.md backup section | mitigate | The doc instructs to record the OFFLINE BACKUP LOCATION but not the key material itself. Reviewer must confirm no key bytes are pasted into the doc when Phase 12 lands |
| T-11-17 | Tampering | `v*-rc.*` tag pushed by accident triggers a real release | accept | Same workflow runs for both; the `prerelease: true` flag and draft mode prevent user impact. `gh release delete` cleanup step in dry-run procedure handles accidents |
| T-11-18 | Repudiation | Dry-run results not captured — future operators can't verify the gate was met | mitigate | Task 3 explicitly requires `11-DRY-RUN-LOG.md` to be created and committed. CI-13 is satisfied only when this artifact exists with evidence |
| T-11-19 | Denial of Service | Apple notary outage during dry-run blocks Phase 12 work | accept | Pitfall #8: notary outages happen. Re-tag and re-run; the dry-run is not on a critical-path schedule |
</threat_model>

<verification>
1. `.github/workflows/release.yml` has the `softprops/action-gh-release` step appended with `draft: true`, SHA-pinned, YAML valid
2. `.github/workflows/release.yml` no longer contains the `REPLACE_WITH_V6_0_0_SHA` placeholder from plan 11-01
3. `docs/release/ci-secrets.md` exists with all nine secret sections, each containing rotation + loss-impact sub-sections
4. `.planning/phases/11-ci-notarized-release/11-DRY-RUN-LOG.md` exists and records a successful (or documented-failed) `v2.2.0-rc.1` workflow run with the workflow URL, asset checksums, and stapler validation output
5. `gh release view v2.2.0-rc.1 --repo mitgor/mdviewer` shows draft=true, prerelease=true, with the DMG as an asset (or, if the dry-run was cleaned up, the log records that the release/tag was deleted post-verification)
</verification>

<success_criteria>
- CI-09: Released as draft by default — workflow has `draft: true` hardcoded; the only path to a published release is the developer manually flipping it in the GitHub UI
- CI-12: All nine secrets documented in `docs/release/ci-secrets.md` with role minimums and rotation procedures
- CI-13: First end-to-end dry-run against `v2.2.0-rc.1` produced a notarized stapled DMG attached to a draft pre-release; evidence captured in `11-DRY-RUN-LOG.md`
- All 13 CI requirements (CI-01..CI-13) for Phase 11 are now satisfied across plans 11-01 and 11-02
- The pipeline is cleared for the real `v2.2.0` cut (which happens in the milestone closeout, not in this phase)
</success_criteria>

<output>
After completion, create `.planning/phases/11-ci-notarized-release/11-02-SUMMARY.md` summarizing the publish step addition, the secrets doc, and the dry-run outcome. Reference the dry-run log file by path.
</output>
